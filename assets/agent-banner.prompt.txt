Edit the supplied image. Change ONLY the mathematical equations into programming source code related to AI agents. Preserve exactly the composition, wide aspect ratio, black hole position and shape, turbulent white light filaments, glow, star field, black background, monochrome palette, and the continuous curved 3D surface sweeping in from the right and compressing into the event horizon. Replace ALL physics equations, integrals, Greek symbols and mathematical notation with dense believable monospace source code: async functions, indented blocks, braces, parentheses, quoted strings, conditionals, loops and tool calls. Example snippets to distribute naturally: "async def run_agent(task):", "context = memory.retrieve(task)", "plan = await agent.plan(context)", "for step in plan.steps:", "result = await tools.execute(step)", "memory.append(result)", "if result.is_complete:", "return result", "await agent.reflect(feedback)", "messages.append(response)", "class Agent:", "self.tools = tools". These snippets should form a cohesive flowing tapestry of code, not isolated labels. Code is white and silver only, with fine luminous monospace typography; absolutely no colorful syntax highlighting. Crucially the code must adhere to the SAME curved spacetime surface and foreshortening as the original equations: large and readable at the far right foreground, becoming increasingly tiny, dense and warped near the black hole. Keep the original density, delicate texture and cinematic light. No editor window, no panels, no captions, no title, no watermark, no new objects. This is a precise text-content replacement, not a redesign.
